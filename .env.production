# Variabili d'ambiente per PRODUZIONE

# URL del sito (IMPORTANTE: cambia questo con il tuo dominio)
NEXT_PUBLIC_SITE_URL=https://concorso.capitalelavoro.it

# Node Environment
NODE_ENV=production

# Porta (di default 3000, cambia se già occupata)
PORT=3000

# Disabilita telemetria Next.js
NEXT_TELEMETRY_DISABLED=1

# Path per salvare le submissions (relativo alla root del progetto)
# In produzione: /var/www/concorso-fotografico/submissions
SUBMISSIONS_PATH=./submissions
